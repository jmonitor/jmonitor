# yaml-language-server: $schema=../../vendor/symfony/dependency-injection/Loader/schema/services.schema.json
# see https://symfony.com/doc/current/reference/configuration/framework.html
framework:

    # https://www.cloudflare.com/ips/
    trusted_proxies: '%env(csv:TRUSTED_PROXIES)%'
    trusted_headers: ['x-forwarded-for', 'x-forwarded-proto', 'x-forwarded-port', 'x-forwarded-host']

    secret: '%env(APP_SECRET)%'

    # Note that the session will be started ONLY if you read or write from it.
    session:
        # ID of the service used for session storage
        # NULL means that Symfony uses PHP default session mechanism
        handler_id: '%env(REDIS_SESSION_DSN)%'
        cookie_samesite: lax
        cookie_secure: auto
        cookie_domain: '%env(APP_DOMAIN)%'
        name: 'JMONITOR_SESSID_%env(APP_ENV)%'
        gc_probability: 1 # unused with Redis
        gc_divisor: 1000 # unused with Redis
        cookie_lifetime: 0
        # Idle time before the session expires. Users are not logged out thanks to remember-me.
        gc_maxlifetime: 1800 # 30 minutes.

    # https://symfony.com/doc/current/reference/configuration/framework.html#configuration-framework-http_cache
    http_cache:
        enabled: false
        debug: '%kernel.debug%'
        default_ttl: 0
        allow_reload: true
        allow_revalidate: true

when@test:
    framework:
        test: true
        session:
            storage_factory_id: session.storage.factory.mock_file

when@dev:
    framework:
        http_client:
            default_options:
                verify_peer: false
