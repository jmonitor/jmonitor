framework:
    cache:
        # Tests should not need a running Redis: an in-memory pool is enough, since nothing
        # in the test suite depends on cached data surviving between requests or processes.
        app: cache.adapter.array
