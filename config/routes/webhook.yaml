webhook:
    resource: '@FrameworkBundle/Resources/config/routing/webhook.php'
    prefix: /webhook
    host: "webhook.%app.domain%"
    # The Stripe webhook only exists in the cloud edition.
    condition: "env('APP_EDITION') == 'cloud'"
