<span {{ attributes.defaults({class: 'badge ' ~ cssClass}) }}>
    {% if icon %}{{ ux_icon(icon, {width: '1.1em', height: '1.1em'}) }}{% endif %}
    {% block content %}{{ badge ? badge.label }}{% endblock %}
</span>
