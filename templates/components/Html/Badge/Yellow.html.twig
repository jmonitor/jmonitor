<twig:Html:Badge:Badge icon="{{ icon ? 'flowbite:exclamation-circle-outline' }}" class="{{ attributes.defaults({class: 'badge-warning'}).all().class }}">
    {{ block(outerBlocks.content) }}
</twig:Html:Badge:Badge>
