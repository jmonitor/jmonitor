<div {{ attributes }}>
    {{ form_start(form, {attr: {class: 'd-inline-block', onchange: 'this.requestSubmit()'}}) }}
        {{ form_row(form.range, {row_attr: {class: 'mb-0'}}) }}
    {{ form_end(form) }}
</div>
