{# @var bag \App\Metrics\Dto\Bag\Postgres\PostgresDatabaseBag #}
{% set bag = dto.value %}

<div class="display-6">{{ dto.formattedValue ?? '—' }}</div>
<div class="text-muted text-sm mt-1">tables in schema {{ bag.schema ?? 'public' }}</div>
